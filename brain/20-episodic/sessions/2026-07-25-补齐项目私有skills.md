---
type: session
created: 2026-07-25
tool: cursor
tags: [情景记忆, 会话日志, skills]
---

# 2026-07-25 · 补齐项目私有 skills

## 目标

回答「为什么脚手架没有 skills」，并补上项目私有 skills 与「沉淀 → 铸造 skill」管线。

## 做了什么

1. **查证**：`.agent/skills/`（单数）下 5 个 openspec skill 未被加载——本次会话挂载的 skill 只有 `.agents/skills/` 下 7 个设计类技能。确认 Cursor/Codex 只扫描 `.agents/` `.cursor/` `.claude/` `.codex/` 下的 `skills/`。
2. **新建 6 个 skill**（`.agents/skills/`）：
   - 记忆类：`memory-capture`、`adr-write`、`session-wrap`、`skill-forge`
   - 项目私有代码类（带 `paths` 限定）：`ws-contract-check`、`rag-guardrails`
3. **建立铸造管线**：`memory-consolidation` 第 2 步新增分流去向「反复出现 ≥3 次的套路 → 铸成 skill」，判断标准写进 `skill-forge`。
4. **命令改薄壳**：`/收尾` `/沉淀` `/巩固记忆` 改为指向对应 skill，延续「一套真相」原则。
5. 新增 [[skills-index]]；更新 memory-map、daily-usage、HOME、AGENTS、cursor rule。

## 关键决策

- [[ADR-0005-项目私有skills与铸造管线]]

## 为什么这么做

文档不会自动到场——AI 得先想起来去读；skill 的 `description` 让模型能按情境自动选中。`ws-contract-check` 和 `rag-guardrails` 用 `paths` 限定，只在改对应目录时出现，避免污染无关任务的上下文。

## 遗留 / 下一步

- 新 skill 需重开会话才加载。
- `.agent/skills/` 的 openspec skills 是否迁移，待用户确认（未擅自改动既有工具链）。
