---
type: session
created: 2026-07-25
tool: cursor
tags: [情景记忆, 会话日志, 记忆库]
---

# 2026-07-25 · 脚手架 v2 加固

## 目标

按对 ai-bom-companion 的评审结论，加固本仓记忆脚手架（不改动 aibom 仓）：入口去重、inbox、模块反向指针、斜杠命令、日常用法闭环。

## 做了什么

1. 新增真相文件 [[memory-map]]；根 `AGENTS.md` 与 `.cursor/rules/memory-protocol.mdc` 改为薄索引。
2. 引入 `brain/_inbox/`、`_dashboards/`（Dataview）、`daily-usage` 手册；更新三工作流与宪法 v1.1。
3. 六个模块嵌套 `AGENTS.md`；`project-map` 补代码锚点。
4. Cursor 命令：`/收尾` `/沉淀` `/巩固记忆` `/回顾进度`。
5. `brain/.obsidian` 轻量配置 + `.gitignore`；ADR-0004。

## 关键决策

- [[ADR-0004-入口去重与inbox两段式]]（补充 ADR-0003，不取代）

## 遗留 / 下一步

- 用户用 Obsidian 打开 `brain/`，安装 Dataview 后看仪表盘。
- 业务代码未提交改动仍在工作区，与脚手架无关，待用户决定是否提交。
- open-questions 中 target/ 与 DocumentController 项仍待处理。
