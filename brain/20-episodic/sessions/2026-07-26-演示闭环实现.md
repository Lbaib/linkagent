---
type: session
created: 2026-07-26
tool: cursor
tags: [情景记忆, 会话日志, 演示闭环]
---

# 2026-07-26 · 演示闭环实现（访客 AI → 转人工 → 单客服接管）

## 目标

打通可演示业务闭环：访客匿名提问 → 真实 RAG 流式回复 → 转人工 → 单客服真实接管；顺带清理根 `.gitignore` / `target/` 与知识库上传锚点。

## 做了什么

1. 分支 `feat/demo-closed-loop`；设计与计划见 `docs/superpowers/specs|plans/2026-07-26-demo-closed-loop-*`。
2. 子代理驱动 Task 1–11：common-core role claim、chat-server 帧转发、customer-service 编排/路由、ai-rag 密钥外置、访客/客服前端真实 WS。
3. 终审修复：`TRANSFER_AGENT` 幂等；`JwtUtils` 签名密钥 fail-closed（`LINKEDAGENT_JWT_SECRET`）。
4. 全量测试：后端约 45+、前端 21 通过；文档/openspec/记忆同步（Task 11 Phase A）。

## 关键决策

- JWT `role`（visitor/agent）写入 claim；访客 token → `sessionStorage.visitorToken`，客服 → `localStorage.accessToken`。
- 模型不可用下发 `ERROR`，禁止伪造回答。
- Vite `/ws` 代理到 `8081`，前端仍用 `window.location.host`（不经网关）。
- 详见后续巩固归档的 [[ADR-0008-JWT签名密钥外置fail-closed]] 与 [[客服业务域]]。

## 遗留

- 双浏览器 e2e（Task 11 Steps 2–3）待人工验证，未宣称演示通过。
- 离线留言落库、满载排队位次、token 级 LLM 流式未纳入本次。
