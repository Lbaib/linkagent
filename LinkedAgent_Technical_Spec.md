# LinkedAgent 智能客服平台技术规格书

## 1. 项目概述与目标 (Project Overview & Goals)

**LinkedAgent** 是一款旨在“连接客户与服务”的新一代智能客服平台。通过将现代前端体验、稳定的 Java 微服务后端与前沿的 AI 大模型深度结合，LinkedAgent 致力于大幅提升企业客服的接待人效，同时为客户带来极致便利的服务体验。

**核心目标：**
*   **智能化提效**：通过 RAG (检索增强生成) 与大模型技术，让 AI 能够精准解答绝大部分常见客户问题，拦截基础咨询，释放人工客服精力。
*   **无缝人机协作**：当复杂问题超出 AI 能力边界时，系统能够顺畅、无痕地将对话与完整上下文流转至人工坐席，实现人机高效接力。
*   **高性能与现代体验**：依托长连接通信与流式响应机制，降低回复延迟；同时提供直观、美观的现代化前端操作界面。

## 2. 系统架构设计 (System Architecture Design)

本项目采用前后端分离的现代微服务架构，基于 Spring Cloud 构建，以满足高并发和低延迟的客服业务场景。

### 2.1 总体微服务拓扑图与组件说明
系统自上而下分为展示层、网关层、微服务业务层和数据存储层。各微服务组件职责明确，保证了系统的高可用与可扩展性。

*   **API Gateway (Spring Cloud Gateway)**: 系统的 HTTP 统一流量入口。负责普通接口路由分发与跨域处理。所有静态资源和传统的短连接 API 请求均由此转发。
*   **Chat-Server (独立 WebSocket 长连接网关)**: 专职维护客户端与服务器的双向 WebSocket 长连接。前端 `ws://` 流量**绕过主网关直接连入该服务暴露的独立端口**，实现长短连接的真正物理隔离，彻底避免高并发长连接拖垮正常的 HTTP 路由网关。
*   **Customer-Service (客户会话与路由服务)**: 核心业务微服务。负责客户会话生命周期管理、聊天记录落库、人工客服排队分配与路由调度逻辑。
*   **AI-RAG-Service (AI 大模型与知识检索服务)**: 负责封装并对接外部大模型 API、基于 `pgvector` 的知识片段向量检索与召回、以及 Prompt 组装。它是智能问答的核心引擎。
*   **System-Management (系统与后台管理服务)**: 负责后台系统级配置、知识库文档的上传/解析与切片管理、客服人员账号的 CRUD 等基础功能。

### 2.2 核心架构防腐与通信机制
*   **外部大模型防腐层设计 (Anti-corruption Layer)**: 在 `AI-RAG-Service` 中设立统一的接口防腐适配器。未来若需从公共大模型无缝切换至其他厂商（或本地私有模型），只需更换对应的实现类，内部业务逻辑零感知。
*   **内部微服务通信**: 系统内部采用 **OpenFeign** 进行同步通信调用，降低架构开发复杂度，提升研发迭代效率。
*   **异步解耦通信**: 组件间的异步解耦和削峰初期采用 **Redis (List/PubSub)**。为支持架构平滑演进，代码设计上将预留 **RocketMQ/Kafka** 的标准消息接口。

### 2.3 高可用与容错策略
*   **服务治理**: 采用 **Nacos** 作为统一的服务注册发现与分布式配置中心。
*   **容错与限流熔断**: 在核心脆弱链路（特别是对外部大模型 API 的调用处）引入熔断降级与限流策略（如使用 Sentinel），防止恶意请求引发雪崩及 API 额度超刷。
*   **数据存储分层模型**: 采用冷热分层架构。在线会话态、高频热数据、排队队列主要维护在 Redis 中；而持久化的历史聊天记录、知识库原文/向量及系统账单等，则落盘至 PostgreSQL 以确保数据不丢失。

## 3. 技术选型与数据存储 (Tech Stack & Data Storage)

为了保证系统不仅“现代好用”，而且能支撑企业级的高并发请求，技术栈选型如下：

### 3.1 核心技术栈
*   **前端 (Frontend)**：基于 **React** 框架，结合 **TailwindCSS** 进行原子化样式构建，UI 组件库采用设计精良的 **shadcn/ui**。这一组合保证了控制台界面的极高美观度、出色的交互体验及长期的可维护性。
*   **后端 (Backend)**：采用 **Java** 语言与 **Spring Boot** 框架。微服务治理层面使用 **Spring Cloud** (配合 Nacos) 体系。
*   **AI 引擎 (AI Engine)**：业务层不自建底层模型，而是灵活对接优秀的**公共大模型 API**，专注于 Prompt 编排与 RAG 业务流程。

### 3.2 数据存储选型
*   **核心关系型数据**：使用 **PostgreSQL**。不仅用于存储结构化的业务数据（如账单、会话记录），更关键的是利用其 **`pgvector`** 扩展插件，直接在同一个库中实现知识库文档的向量存储与高维相似度检索，大幅降低系统运维复杂度。
*   **高频缓存与状态**：使用 **Redis**。承载 WebSocket 用户 Session 路由表、在线客服状态、排队队列、限流计数器以及高频系统配置。

## 4. 核心业务流程 (Core Workflows)

### 4.1 客户端接入与鉴权机制
1. **访客身份初始化**：针对无账号的普通访客，前端加载时先请求业务 API 换取一个临时的匿名 JWT Token。
2. **直连与鉴权**：前端携带该 JWT（放置在 WebSocket URL 的 Query 参数中，以解决浏览器原生限制）向独立的 `Chat-Server` 直接发起连接。`Chat-Server` 解析 JWT 鉴权通过后，正式建立双向通信通道。

### 4.2 核心场景一：AI 智能问答流程
1. **消息接收与异步持久化**：用户发出的消息通过 WebSocket 到达 `Chat-Server`。系统优先将消息写入 Redis 暂存以保证极高的吞吐量，随后再由 `Customer-Service` 异步批量落盘至 PostgreSQL，解决并发写瓶颈。
2. **知识检索 (RAG)**：消息同步给 `AI-RAG-Service`。考虑到模型通用性，文档切片后会统一映射为标准化维度的向量（例如 OpenAI 的 1536 维），存入 PostgreSQL (`pgvector`) 中进行 KNN 相似度检索召回。
3. **大模型推理与纯流式推送**：将召回的背景知识和问题组合成 Prompt 调用大模型。生成的文本流**统一采用纯 WebSocket 数据帧**进行持续分片下发（打字机效果），不再混用 HTTP SSE 协议，大幅简化前端接入。

### 4.3 核心场景二：人工接管与排队路由
转人工触发条件采用双轨制：**主动触发**（用户点击“转人工”按钮）与**被动触发**（AI 判断自身无法回答、置信度极低或察觉用户情绪波动）。
1. **状态切换**：会话状态由“AI 托管”立即切换为“人工排队”。
2. **结合负载的轮询分配**：`Customer-Service` 获取在线客服池。分配时优先过滤掉当前接待量已达设定上限（如 5 人）的客服，再在剩余可用客服中进行**简单轮询 (Round-Robin)** 快速分配。
3. **排队与留言保障**：若过滤后可用客服为空（全员达接待上限），客户进入 Redis 队列排队，前端实时更新排队位次；若全员离线，则转入离线工单留言流程。

### 4.4 核心场景三：人工客服双向沟通与上下文漫游
1. **上下文无缝漫游**：客服接单后，工作台会立刻同步拉取该用户之前的聊天记录（**包含刚刚用户与 AI 沟通的全部上下文**），让客服无需让客户重复描述问题即可开展服务。
2. **实时互发与保活**：客服工作台与客户的聊天经由 `Chat-Server` 路由，实现低延迟双向通信。双方定时发送 Ping/Pong 心跳包维持长连接活跃。

### 4.5 异常保障与风控机制
*   **断线重连与防丢消息**：遇到移动端网络切换导致断网，前端具备指数退避重连机制；系统采用消息 ACK 确认机制，对于未收到 ACK 的消息在重连后予以自动补发。
*   **内容风控**：所有发出的消息到达 `Chat-Server` 后，将优先交由后台业务拦截器进行敏感词库过滤，确认合规后再落库及推送下发。
*   **会话结束与评价**：服务结束（客户关闭或客服结单）后，系统向客户推送满意度评价卡片，并归档该会话生成持久化的服务工单记录。

## 5. 非功能性需求与架构演进 (Non-Functional & Future Evolution)

### 5.1 高并发与低延迟保障
*   客服系统的核心痛点是并发长连接与实时消息收发。系统初期设计采用 **Redis** 作为轻量级的消息发布/订阅与异步任务队列，以满足中等规模下的并发性能要求。
*   对于大模型的长文本生成，采用 **SSE (Server-Sent Events)** 机制向客户端推流，首字响应时间控制在极短范围内，提升用户体验。

### 5.2 架构演进规划 (Evolution Plan)
*   **极限削峰演进**：当平台流量进一步激增，系统预留的异步接口将无缝接入专业消息中间件（如 **RocketMQ** 或 **Kafka**），替代 Redis 承担海量消息的异步落库与极端削峰。
*   **多租户支持 (SaaS化)**：目前版本优先保障单体企业的高可用运行；但底层模型已预留租户隔离标识。未来可平滑演进为标准 SaaS 平台，支持多企业入驻的数据逻辑隔离。
